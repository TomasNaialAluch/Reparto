rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura para usuarios autenticados (Google o anónimos)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
